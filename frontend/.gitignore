# Playwright artifacts
playwright-report/
.playwright/

# Playwright traces and reports
*.trace
playwright-report.zip

# Local test outputs
test-results/

# Storybook build outputs (already ignored at repo root but keep local)
storybook-static/

# Node & editor
node_modules/
.DS_Store
.vscode/
.idea/

tsconfig.tsbuildinfo
dist-cli

# ADR-021 Phase 2: native static export output and its generated build tree.
/out/
.native-build/
# ADR-021 Phase 2: generated (not tracked) link to repo-root getwrite-config; created by scripts/ensure-config-link.mjs
/getwrite-config
